# Phase 4 子任务清单 - M1: MCP MVP

> 来源：ERP集成规划任务清单 (T-01 ~ T-07)
> 里程碑：M1: MCP MVP
> 预计完成：Week 2

---

## 子任务列表

### ST-001: MCP 协议调研与文档阅读
**父任务**: T-01 MCP 兼容性验证
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 阅读 MCP 官方文档 https://modelcontextprotocol.io/
2. 理解 MCP Server-Client 架构
3. 确认 Python MCP SDK (`mcp-python-sdk`) 的 API 稳定性

**验收标准**:
- [x] 输出 MCP 协议要点总结（Markdown）
- [x] 确认 SDK 版本号：v1.27.1

---

### ST-002: MCP Inspector 工具安装与配置
**父任务**: T-01 MCP 兼容性验证
**执行者**: AI Agent
**预估时间**: 20min

**执行步骤**:
1. 全局安装 `@modelcontextprotocol/inspector`
2. 验证 inspector 命令可用
3. 创建测试 MCP Server 骨架

**验收标准**:
- [ ] `npx @modelcontextprotocol/inspector` 命令执行成功
- [ ] 测试 Server 可被 Inspector 发现

---

### ST-003: MCP Server 项目结构创建
**父任务**: T-02 Kingdee MCP Server 项目初始化
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 创建项目目录 `/services/kingdee-mcp-server/`
2. 初始化 `pyproject.toml` 或 `requirements.txt`
3. 创建标准 MCP Server 入口文件 `server.py`
4. 配置 `.env.example` 模板

**验收标准**:
- [x] 目录结构符合 MCP 规范
- [x] `python -m kingdee_mcp_server.server` 可启动

---

### ST-004: 金蝶 SDK 集成到 MCP Server
**父任务**: T-02 Kingdee MCP Server 项目初始化
**执行者**: AI Agent
**预估时间**: 20min

**执行步骤**:
1. 将 `kingdee_webapi_sdk` 作为依赖添加
2. 创建 `kingdee_client.py` 封装 SDK 初始化
3. 从 `.env` 读取金蝶 API 凭证

**验收标准**:
- [x] SDK 可成功初始化
- [x] 凭证从环境变量读取

---

### ST-005: 实现 query_erp_data Tool 定义
**父任务**: T-03 实现 query_erp_data Tool
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 定义 Tool Schema（参数：form_id, filter_string, field_keys, limit）
2. 使用 `@mcp.tool()` 装饰器注册
3. 调用 `kingdee_webapi_sdk` 的 `execute_bill_query` 方法
4. 返回格式化的 JSON 结果

**验收标准**:
- [x] Tool 在 MCP Inspector 中可见
- [x] 参数 Schema 正确展示

---

### ST-006: query_erp_data 单元测试
**父任务**: T-03 实现 query_erp_data Tool
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 创建 `tests/test_query_tool.py`
2. Mock 金蝶 API 响应
3. 测试正常查询、空结果、错误处理

**验收标准**:
- [x] `pytest tests/test_query_tool.py` 通过
- [x] 覆盖率 > 80%

---

### ST-007: 实现 create_erp_bill Tool 定义
**父任务**: T-04 实现 create_erp_bill Tool
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 定义 Tool Schema（参数：form_id, json_data）
2. 调用 SDK 的 `save` 和 `submit` 方法
3. 包装为原子操作，确保事务一致性
4. 返回创建结果（单号、状态）

**验收标准**:
- [x] Tool 在 MCP Inspector 中可见
- [x] save + submit 作为原子操作执行

---

### ST-008: create_erp_bill 单元测试
**父任务**: T-04 实现 create_erp_bill Tool
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 创建 `tests/test_create_tool.py`
2. Mock 金蝶 API 响应
3. 测试成功创建、验证失败、网络错误

**验收标准**:
- [x] `pytest tests/test_create_tool.py` 通过
- [x] 验证原子性（失败时回滚）

---

### ST-009: 实现 upload_erp_attachment Tool 定义
**父任务**: T-05 实现 upload_erp_attachment Tool
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 定义 Tool Schema（参数：file_path, form_id, bill_no）
2. 调用 SDK 的附件上传接口
3. 返回附件 ID

**验收标准**:
- [x] Tool 在 MCP Inspector 中可见
- [x] 支持常见文件格式（PDF, Excel, Image）

---

### ST-010: MCP Server 安全配置
**父任务**: T-06 MCP Server 安全配置（凭证隔离）
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 确保凭证仅从环境变量读取，不硬编码
2. 配置日志脱敏（过滤 app_id, app_secret）
3. 响应中不返回敏感信息
4. 添加 `.env` 到 `.gitignore`

**验收标准**:
- [x] 代码审计无凭证泄露
- [x] 日志中无明文凭证（使用 logger，不打印敏感字段）
- [x] API 响应无敏感字段
