# Phase 4 子任务清单 - M2: Agent 集成

> 来源：ERP集成规划任务清单 (T-08 ~ T-12)
> 里程碑：M2: Agent 集成
> 预计完成：Week 3

---

## 子任务列表

### ST-011: MCP Server 分页截断机制
**父任务**: T-07 MCP Server 分页截断机制
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 在 `query_erp_data` 中添加 `limit` 参数默认值（100）
2. 超过 limit 时返回截断提示
3. 添加 `has_more` 字段到响应

**验收标准**:
- [ ] 默认 limit=100
- [ ] 大数据集返回 `has_more: true`
- [ ] Token 消耗可控

---

### ST-012: Hermes Agent MCP Client 配置文件
**父任务**: T-08 Hermes Agent MCP Client 配置
**执行者**: AI Agent
**预估时间**: 20min

**执行步骤**:
1. 在 `config/` 下创建 `mcp_servers.yaml`
2. 注册 Kingdee MCP Server 连接信息
3. 配置超时、重试策略

**验收标准**:
- [ ] 配置文件格式正确
- [ ] Server 配置可被 Agent 读取

---

### ST-013: Agent MCP 连接代码实现
**父任务**: T-08 Hermes Agent MCP Client 配置
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 在 `hermes-agent` 中添加 MCP Client 模块
2. 实现连接初始化、Tool 发现
3. 实现 Tool 调用封装

**验收标准**:
- [ ] Agent 启动时自动连接 MCP Server
- [ ] Tool 列表正确加载

---

### ST-014: Grounding Prompt 设计与实现
**父任务**: T-09 Grounding Prompt 设计
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 编写 System Prompt：写操作前必须先查询
2. 添加示例对话（查询→确认→建单）
3. 配置到 Agent 的 persona 中

**验收标准**:
- [ ] Prompt 包含强制查询规则
- [ ] Agent 在写操作前自动执行查询

---

### ST-015: MCP 调用链路 E2E 测试编写
**父任务**: T-10 MCP 调用链路 E2E 测试
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 创建 `tests/e2e/test_mcp_flow.py`
2. 模拟用户输入："查询 M001 物料库存"
3. 验证 Agent 调用 `query_erp_data`
4. 模拟建单流程，验证完整链路

**验收标准**:
- [ ] E2E 测试覆盖查询→建单流程
- [ ] 测试可重复执行

---

### ST-016: ERP 操作审计日志实现
**父任务**: T-11 ERP 操作审计日志
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 创建 `audit_logger.py` 模块
2. 记录：时间戳、用户 ID、操作类型、参数、结果
3. 写入日志文件或数据库

**验收标准**:
- [ ] 每次操作生成审计记录
- [ ] 日志格式可解析

---

### ST-017: 写操作二次确认机制实现
**父任务**: T-12 写操作二次确认机制
**执行者**: AI Agent
**预估时间**: 40min

**执行步骤**:
1. 在 `create_erp_bill` 中添加 `dry_run` 模式
2. Agent 返回预览信息："即将创建 X 条订单，请确认(Y/N)"
3. 用户确认后才执行实际写入

**验收标准**:
- [ ] dry_run 模式返回预览
- [ ] 实际写入需用户确认

---

### ST-018: Agent 集成测试与调优
**父任务**: T-08, T-09, T-10
**执行者**: AI Agent
**预估时间**: 30min

**执行步骤**:
1. 运行完整测试套件
2. 修复发现的 Prompt 问题
3. 优化 Tool 调用延迟

**验收标准**:
- [ ] 所有测试通过
- [ ] P95 延迟 < 3s