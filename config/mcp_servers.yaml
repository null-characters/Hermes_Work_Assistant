# MCP Servers Configuration
# 此文件定义 Hermes Agent 可连接的 MCP Server 列表
# 参考: https://modelcontextprotocol.io/docs/concepts/architecture

# =============================================================================
# Kingdee ERP MCP Server
# =============================================================================
kingdee-erp:
  # 传输方式: sse (HTTP/SSE 传输，用于远程连接)
  transport: sse

  # SSE 端点 URL
  url: http://kingdee-mcp-server:8080/sse

  # 超时配置（秒）
  timeout: 60

  # 重试策略
  retry:
    max_attempts: 3
    backoff_factor: 1.5

  # 启用状态
  enabled: true

  # 描述
  description: "金蝶云星空 ERP MCP Server - 提供查询、建单、附件上传功能"

# =============================================================================
# 未来扩展: 其他 MCP Server
# =============================================================================
# example-server:
#   transport: streamable-http
#   url: http://localhost:8080/mcp
#   headers:
#     Authorization: Bearer ${EXAMPLE_API_KEY}
#   enabled: false